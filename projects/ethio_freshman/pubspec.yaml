name: ethio_freshman
description: "Start - The professional app for Ethiopian Freshman students."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.1
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  # 💡 THIS WAS MISSING: It fixes the analysis_options warning
  flutter_lints: ^3.0.0
  flutter_launcher_icons: "^0.13.1"

# 🧠 ICON CONFIGURATION
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/brain.png"
  min_sdk_android: 21

# 🏗️ FLUTTER ENGINE CONFIGURATION
flutter:
  uses-material-design: true
  assets:
    - assets/images/brain.png